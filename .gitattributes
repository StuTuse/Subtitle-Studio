# 统一换行，避免 Windows/Linux 之间无意义的整文件 diff。
* text=auto eol=lf

# 真正的二进制
*.png binary
*.ico binary
*.pt binary
*.bin binary
*.model binary
*.whl binary

# Windows 批处理必须 CRLF，否则某些命令解析会出问题
*.bat text eol=crlf

# 打包产物与运行期数据永不入库（与 .gitignore 双保险）
dist/** export-ignore
build/** export-ignore
